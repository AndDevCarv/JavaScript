function forEach2(lista, callback) {
    for (let i = 0; i < lista.length; i++){
        
    }
}

const aprovados = ['andre', 'bruno', 'luana']

aprovados.forEach2(function(nome, indice){ 
    console.log(`${nome}, ${indice}`);
})